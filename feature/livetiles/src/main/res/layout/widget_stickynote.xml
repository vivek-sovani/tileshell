<?xml version="1.0" encoding="utf-8"?>
<!-- Full-size sticky-note widget — the raw stored text (see
     WidgetStickyNoteStore), no ViewFlipper (unlike every other widget in this
     batch): there's no separate "back" fact to show, the note text itself is
     the whole content. Both the body and the gear reopen WidgetConfigureActivity
     to edit it (see StickyNoteWidgetRefreshWorker). -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_rounded_background">

    <ImageView
        android:id="@+id/widget_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY" />

    <TextView
        android:id="@+id/widget_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="14dp"
        android:textSize="15sp"
        android:maxLines="8"
        android:ellipsize="end"
        android:textColor="#FFFFFFFF" />

    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_gravity="top|end"
        android:layout_margin="6dp"
        android:src="@drawable/ic_widget_stickynote" />

    <ImageView
        android:id="@+id/widget_settings"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="6dp"
        android:padding="2dp"
        android:src="@drawable/ic_widget_settings" />

</FrameLayout>
